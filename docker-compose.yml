services:
  imgshare_bot:
    build: .
    command: ./imgshare_bot
    environment:
      - DBHOST=localhost
      - DBPORT=5432
      - DBSSL=disable
    env_file: .env
    depends_on:
      - db
  postgresdb:
    image: postgres
    restart: always
    env_file: .env
